main core kaam = scaling

primary network in kubernetes also major work in microservices

servers are called nodes

kuver cluster is private network and node is machine

each server resource usage identifing ( k8 ko chalne ke lye chije jo kam ki hai vo hota hai isme )= master node

metric server = continiously watches how much resources are used cpu, ram, etc are using and tells to master node

HPA, etcd = cluster ki state maintain krta hai ki versions kya hai ye sab data.

<!-- master node - control plane -->

API server - kam ata hai command chalane ke liye kyuki vo apne server pr nhi hota ye kahi bhi ho skta hai isliye api server cmds ko run krne ke liye kam ata hai 


worker node - where pods run 

machine hai jinke uper actual server chalte hai 

<!-- 01 - POD - smallest deployable unit in k8 -->

pods are wrapper around many containers but generallly one container(90 - 95% time 1 hi hota hai). it wraps containers and shares same network and same storage to every container inside it, hence all containers runs on single host or network

k8 jab server pr load ata hai toh pods ko badha deta hai naaki direct containers ko. or pods ke under container chal rhe hote hai toh pods badhate hai toh contianers bhi badh jate hai 

<!-- ingress and services -->

02 - Deployment is a Manager of pods decides = which image to run, how many replicas, resources each pod needs, updates old to new version without downtime -  it maintains number of pods always same pods if one crashes it imediately creates and run new

<!-- labels & services -->
IP changes every time image build that's labels are used to identify each pod and services are used to route requests to which pods you have to hit.

services can be multiple like(auth, notification, posts, user etc);

Deployment can't handle auto scale it only done by manually providing number of replication

autoscaling handled by HPA


<!-- which request will go to which pod it defined as ingress -->

03 - Ingress(Set of Rules and gegulations inforced(implement) by ingress-controller; (ingress is like constitution book)) and Ingress-Controller 

